# {# rr(`\s{10}.*`, "          " + theme.font_mono.name) #}
font_family          monospace
# {# rnum(theme.font_mono.size) #}
font_size            12
remember_window_size false
background_opacity   0.9
background_blur      1
shell                fish

map page_up   scroll_page_up
map page_down scroll_page_down

include colors.conf
