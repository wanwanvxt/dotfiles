#!/bin/env fish

set -gx LD_PRELOAD "$LD_PRELOAD:/usr/\$LIB/libgamemode.so"

set -q GS_ARGS; or set GS_ARGS
set GS_ARGS (string split ' ' -- $GS_ARGS)
exec gamemoderun gamescope --mangoapp $GS_ARGS -- $argv
