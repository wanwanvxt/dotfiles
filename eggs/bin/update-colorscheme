#!/bin/env fish

set wallpaper (realpath "$argv[1]")

if test -f "$wallpaper"
    thaimeleon "$wallpaper" -w "$HOME/.config/yolk/chameleon.rhai"
    yolk sync

    pkill -USR1 -x kitty
    pkill -USR1 -x nvim
else
    echo "Wallpaper not found: $wallpaper"
    return 1
end

